# Documentation dependencies
sphinx>=8.1.3
# Capped below 5.0. Breathe 5.0.0 (2026-09-21) changes C++ declaration
# handling: 168 declarations in this project stop parsing, their symbols are
# never registered, and a later cross-reference to one of them trips
# `assert parent_symbol` in Sphinx's C++ domain. Sphinx dies mid-write.
#
# Worth lifting deliberately rather than by drift -- an unpinned upper bound is
# how a release nobody asked for became a broken build on main.
breathe>=4.36.0,<5
exhale>=0.3.0
nvidia-sphinx-theme>=0.0.8
myst-parser>=4.0.1
sphinx-copybutton>=0.5.2
sphinx-design>=0.6.1
nbsphinx>=0.9.0
numpydoc>=1.5.0
numpy>=1.24.0  # Required for type annotations in Python modules
